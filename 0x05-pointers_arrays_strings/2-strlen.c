#include "main.h"

/**
 * _strlen- returns the lenght of a string
 * @s: the string to be analysed
 * Return: void
 */

int _strlen(char *s)
{
	int lent = 0;

	while (*str++)
	{
		lent++
	}

	return (lent);
}
