#include "main.h"

/**
 * print_last_digit - prints the last digit
 *
 * @n: the number inputed
 *
 * Return: last digit
 *
 */

int print_last_digit(int n)
{
	int ld = n % 10

		if (ld < 0)
		{
			_putchar(-ld + '0');
			return (-l);
		}
		else
		{
			_putchar(l + '0');
			return (l);
		}
}
